{
  // The settings are the repository's, in /biome.jsonc, shared by every Node module (ADR-0012).
  "$schema": "./node_modules/@biomejs/biome/configuration_schema.json",
  "root": false,
  "extends": "//"
}
